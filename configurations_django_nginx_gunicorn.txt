Nginx Configuration:


server {
    listen 80;
    server_name yourdomain.com;  # Remplacez par votre nom de domaine ou adresse IP

    location / {
        proxy_pass http://127.0.0.1:8000;  # Proxy vers Gunicorn
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /path/to/your/project/static/;  # Remplacez par le chemin vers votre dossier static
    }

    location /media/ {
        alias /path/to/your/project/media/;   # Remplacez par le chemin vers votre dossier media
    }
}


Gunicorn Configuration:


gunicorn --workers 3 --bind 127.0.0.1:8000 gestion_notes_site.wsgi:application


Django settings.py Configuration:


# settings.py
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'gestion_notes',
        'USER': 'gestuser',
        'PASSWORD': 'yourpassword',
        'HOST': '127.0.0.1',
        'PORT': '3306',
    }
}

STATIC_URL = '/static/'
STATICFILES_DIRS = [
    BASE_DIR / "static",
]
